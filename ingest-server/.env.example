# Ingest Server Configuration
# Copy this file to .env and update with your values

# Server bind address (default: 127.0.0.1 for behind Caddy, or 0.0.0.0 for direct access)
BIND_ADDRESS=127.0.0.1

# Server port
# - 3091: Default (for use behind Caddy)
# - 3090: Direct access (without Caddy)
PORT=3091

# Note: For HTTPS/HTTP/2, use Caddy reverse proxy
# Run: USE_CADDY=true ./run.sh
